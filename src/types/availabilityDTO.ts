export interface availabilityResponseDTO {
    id: string,
    userId: string,
    time_slot: string,
}

export interface availabilityDTO {
    user: string,
    userId:string,
    time_slot: string,
}